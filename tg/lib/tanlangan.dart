import 'package:flutter/material.dart';

class Tanlangan extends StatelessWidget {
  const Tanlangan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("tanlangan page"),),);
  }
}