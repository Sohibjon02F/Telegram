import 'package:flutter/material.dart';

class GrouPage extends StatelessWidget {
  const GrouPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("guruhlar"),),);
  }
}